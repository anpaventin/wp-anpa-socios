# Normalise line endings to avoid CRLF/LF churn across Windows/Linux.
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary

# Golden-file email oracle: compared byte for byte, so no EOL conversion ever.
tests/golden/*.txt -text
